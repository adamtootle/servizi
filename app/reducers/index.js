const schedules = require('./schedules');
const plans = require('./plans');
const player = require('./player');
const ui = require('./ui');

module.exports = {
  schedules,
  plans,
  player,
  ui,
};
